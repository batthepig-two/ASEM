kind = "web"
previewPath = "/"
title = "ARK Mutation Helper"
version = "1.0.0"
id = "artifacts/ark-mutation-helper"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22752

[services.development]
run = "pnpm --filter @workspace/ark-mutation-helper run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ark-mutation-helper", "run", "build" ]
publicDir = "artifacts/ark-mutation-helper/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22752"
BASE_PATH = "/"
